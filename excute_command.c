#include "monty.h"

/**
 * excute_command - excute the command
 * @tokens: tokens
 * @tokens_len: tokens length
 * @line_number: line number
 * Return: EXIT_SUCCESS on success.
 */
int excute_command(char **tokens, int tokens_len, int line_number)
{
    printf("excuting line: %d\n", line_number);
    return (EXIT_SUCCESS);
    tokens = tokens;
    tokens_len = tokens_len;
}
